cocos2d-js
1、环境搭建
2、入口
   创建：命令行 cocos new -l js -d 绝对路径名
   生成：在生成的项目里 用命令行打开该窗口 cocos run -p web
   多平台编译：
   cocos compile -p web -m release
   cocos compile -p android -m release
3.在project.json 中的"jsList"把src中的.js文件关联起来cos
 cocoscooc
  在入口处执行代码：
   main.js   coco
  代码写在app.js中 

